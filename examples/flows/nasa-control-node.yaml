# nasa-control-node.yaml
# Samsung NASA HVAC 디바이스 제어 플로우 (nasa-control 노드 사용)
#
# nasa-control 노드는 Samsung NASA 에이전트에 직접 제어 명령을 전달한다.
# bridge 노드 + NASAAdapter 조합을 단일 노드로 대체하며,
# 간편 형식(power, mode 등)을 자동으로 set_multiple 명령으로 변환한다.
#
# ┌──────────────────────────────────────────────────────────────────┐
# │                        데이터 흐름                                │
# ├──────────────────────────────────────────────────────────────────┤
# │                                                                  │
# │  [제어 경로 - MQTT → NASA]                                       │
# │    mqtt-cmd ──→ nasa-ctrl ──→ result-pub                        │
# │                                                                  │
# │  [에러 → 로깅]                                                   │
# │    *:error ──→ error-logger                                      │
# │                                                                  │
# └──────────────────────────────────────────────────────────────────┘
#
# nasa-control 노드 설정:
#   agent_ref:  Samsung NASA 에이전트 이름 (필수)
#   device_id:  기본 디바이스 ID (선택, 메시지에서 오버라이드 가능)
#   timeout:    Process 호출 타임아웃 (선택, 기본값 "5s")
#
# 지원하는 입력 형식:
#
#   1) 간편 형식 - 자동으로 set_multiple 명령으로 변환:
#     { "device_id": "living-room", "power": true, "mode": "cool", "temperature": 24 }
#
#   2) 직접 명령 형식 - NASA Process()에 그대로 전달:
#     { "command": "set_power", "device_id": "living-room", "power": true }
#     { "command": "set_multiple", "device_id": "living-room",
#       "params": { "mode": "cool", "target_temp": 24 } }
#
# 간편 형식에서 인식하는 제어 키:
#   power, mode, temperature, target_temp, fan_speed
#
# 사용법:
#   xflow agent import -f examples/agents/samsung-nasa-serial.yaml
#   xflow agent import -f examples/agents/mqtt-broker.yaml
#   xflow agent import -f examples/agents/stdout-logger.yaml
#   xflow agent import -f examples/agents/error-logger.yaml
#   xflow flow import -f examples/flows/nasa-control-node.yaml
#   xflow flow deploy nasa-control-node
#   xflow flow start nasa-control-node

name: "nasa-control-node"
description: "Samsung NASA HVAC MQTT 제어 - nasa-control 노드 사용"
config:
  track_history: true
  max_history_size: 500
  error_handling: "propagate"
  log_level: "info"
metadata:
  author: "xtra"
  version: "1.0"
  domain: "hvac-control"

required_agents:
  - name: "samsung-nasa-agent"
    type: "samsung-nasa"
  - name: "mqtt-broker"
    type: "mqtt-client"
  - name: "stdout-logger"
    type: "logger"
  - name: "error-logger"
    type: "logger"

nodes:
  # ═══════════════════════════════════════════════════════════════
  # 제어 경로: MQTT → NASA
  # ═══════════════════════════════════════════════════════════════

  # ── 1단계: MQTT 제어 명령 수신 ──
  # 토픽 hvac/control 를 구독하여 에어컨 제어 명령을 수신한다.
  - name: "mqtt-cmd"
    type: "bridge"
    config:
      topics:
        - "hvac/control"
    inputs:
      - "in"
    outputs:
      - "out"
    errors:
      - "error"
    agent_ref:
      agent_name: "mqtt-broker"
      direction: "in"

  # ── 2단계: NASA 에이전트에 제어 명령 전송 ──
  # nasa-control 노드가 간편 형식을 자동으로 set_multiple로 변환한다.
  # bridge + transform 조합 없이 단일 노드로 처리 가능하다.
  #
  # 입력 예시:
  #   { "device_id": "living-room", "power": true, "mode": "cool", "temperature": 24 }
  # 자동 변환:
  #   { "command": "set_multiple", "device_id": "living-room",
  #     "params": { "power": true, "mode": "cool", "target_temp": 24, ... } }
  - name: "nasa-ctrl"
    type: "nasa-control"
    config:
      agent_ref: "samsung-nasa-agent"
      timeout: "5s"
    inputs:
      - "in"
    outputs:
      - "out"
    errors:
      - "error"

  # ── 3단계: 제어 결과를 MQTT로 발행 ──
  - name: "result-pub"
    type: "bridge"
    inputs:
      - "in"
    outputs:
      - "out"
    agent_ref:
      agent_name: "mqtt-broker"
      direction: "out"

  # ═══════════════════════════════════════════════════════════════
  # 에러 로깅
  # ═══════════════════════════════════════════════════════════════

  - name: "error-logger"
    type: "bridge"
    inputs:
      - "in"
    outputs:
      - "out"
    agent_ref:
      agent_name: "error-logger"
      direction: "out"

wires:
  # 제어 경로: MQTT 명령 → NASA → 결과 발행
  - source: "mqtt-cmd:out"
    target: "nasa-ctrl:in"
  - source: "nasa-ctrl:out"
    target: "result-pub:in"

  # 에러 라우팅
  - source: "mqtt-cmd:error"
    target: "error-logger:in"
  - source: "nasa-ctrl:error"
    target: "error-logger:in"
