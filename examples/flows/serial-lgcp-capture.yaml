# serial-lgcp-capture.yaml
# Serial 에이전트로 LGCP 패킷을 캡처하여 콘솔 출력하는 플로우
#
# LGCP 전용 에이전트 없이 범용 Serial 에이전트의 frame 프레이밍으로
# RS-485 버스에서 LGCP 프레임을 조립하고, 로거로 hex 덤프한다.
#
# 구성: Serial 프레임 수신 → 콘솔 출력 (hex 덤프)
#                          ↘ 파일 저장 (raw 프레임)
#
# 출력 예시 (raw hex):
#   562d044455006604445500000204f8...
#
# 사용법:
#   xflow flow import -f examples/flows/serial-lgcp-capture.yaml
#   xflow flow start serial-lgcp-capture

name: "serial-lgcp-capture"
description: "Serial 에이전트 frame 프레이밍으로 LGCP 패킷 캡처"
config:
  track_history: true
  max_history_size: 500
  error_handling: "propagate"
  log_level: "debug"
metadata:
  author: "xtra"
  version: "1.0"
  domain: "protocol-analysis"

required_agents:
  - name: "serial-lgcp"
    type: "serial"
  - name: "stdout-logger"
    type: "logger"

nodes:
  # ── 1단계: Serial 프레임 수신 ──
  # frame 프레이밍이 STX=0x56 감지 후 LEN 기반으로 프레임을 조립하여 전달
  - name: "serial-receiver"
    type: "bridge"
    inputs:
      - "in"
    outputs:
      - "out"
    errors:
      - "error"
    agent_ref:
      agent_name: "serial-lgcp"
      direction: "in"

  # ── 2단계: 콘솔 출력 (실시간 모니터링) ──
  - name: "console-output"
    type: "bridge"
    inputs:
      - "in"
    outputs:
      - "out"
    agent_ref:
      agent_name: "stdout-logger"
      direction: "out"

wires:
  - source: "serial-receiver:out"
    target: "console-output:in"
