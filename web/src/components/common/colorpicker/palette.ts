// 통합 프리셋 팔레트.
//
// 오늘 저장소에는 같은 팔레트의 복사본이 여섯 벌 흩어져 있고 그중 둘이 한 칸씩
// 어긋나 있다. 한 벌로 합치되 **한 색도 잃지 않는다** — 합집합 14색이 전부 이 안에
// 들어 있어야 한다(불변식 I5). 그 14색은 기존 배열들을 실제로 합쳐 세어 얻었다.
//
// @spec SPEC-COLOR-001 §결정 3 (M4)

/**
 * 세 행의 길이가 다른 것은 의도다. 무채색은 **명도 사다리**(8단)이고 색상은
 * **색상환**(10색)이라, 두 축의 자연스러운 눈금 수가 애초에 다르다. 억지로 맞추면
 * 한쪽에 쓰지 않는 칸이 생긴다.
 */

/** 1행 — 무채색 명도 사다리. */
const GRAYS = [
  '#ffffff',
  '#f1f5f9',
  '#94a3b8',
  '#6b7280',
  '#64748b',
  '#374151',
  '#0f172a',
  '#000000',
] as const;

/** 2행 — 기본 색상환. */
const BRIGHTS = [
  '#ef4444',
  '#f97316',
  '#f59e0b',
  '#84cc16',
  '#10b981',
  '#06b6d4',
  '#3b82f6',
  '#6366f1',
  '#8b5cf6',
  '#ec4899',
] as const;

/** 3행 — 같은 색상환의 짙은 단. */
const DARKS = [
  '#b91c1c',
  '#c2410c',
  '#b45309',
  '#4d7c0f',
  '#047857',
  '#0e7490',
  '#1d4ed8',
  '#4338ca',
  '#6d28d9',
  '#be185d',
] as const;

/** 한 행의 칸 수 — 격자 배치와 키보드 이동(→ ↓)이 같은 수를 봐야 한다. */
export const PALETTE_ROWS: readonly (readonly string[])[] = [GRAYS, BRIGHTS, DARKS];

/** 통합 팔레트 28색. 전부 6자리 소문자이며 알파가 없다. */
export const UNIFIED_PALETTE: readonly string[] = [...GRAYS, ...BRIGHTS, ...DARKS];

/**
 * 기존 여섯 배열의 합집합 14색.
 *
 * 여기 적어 두는 것은 참고가 아니라 **가드**다. 팔레트에서 색을 하나 빼는 순간
 * 어떤 사용자의 저장된 색이 프리셋 격자에서 사라지고, 그것은 조용히 일어난다.
 */
export const LEGACY_UNION: readonly string[] = [
  '#3b82f6',
  '#8b5cf6',
  '#06b6d4',
  '#10b981',
  '#f59e0b',
  '#ef4444',
  '#ec4899',
  '#6b7280',
  '#f97316',
  '#64748b',
  '#0f172a',
  '#94a3b8',
  '#ffffff',
  '#000000',
];
